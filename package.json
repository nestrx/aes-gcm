{
  "name": "@nestrx/aes-gcm",
  "version": "0.0.3",
  "description": "The module encryption AES GCM for NestJS framework.",
  "author": "zinzinday@gmail.com",
  "repository": "https://github.com/nestrx/aes-gcm.git",
  "scripts": {
    "build": "rm -rf lib && tsc -p tsconfig.json",
    "prepublish:npm": "npm run build",
    "publish:npm": "npm publish --access public",
    "prepublish:next": "npm run build",
    "publish:next": "npm publish --access public --tag next"
  },
  "devDependencies": {
    "@nestjs/common": "^6.0.0",
    "prettier": "^1.16.4",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^6.4.0",
    "rxjs-compat": "^6.4.0",
    "ts-node": "^8.0.3",
    "typescript": "^3.4.1",
    "@types/node": "^8.10.45"
  },
  "peerDependencies": {
    "@nestjs/common": "^6.0.0"
  }
}
